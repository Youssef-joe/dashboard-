import { Inter } from 'next/font/google';
import { Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'], weight: ['400'] }); // Add weight here
export const lusitana = Lusitana({ subsets: ['latin'], weight: ['700'] });